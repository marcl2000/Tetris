#include <iostream>
#include <vector>
#include <cstddef>
#include "shape.h"
#include "level.h"

using namespace std;

Shape * Level::createShape(string name, bool heavy_flag, bool gd_on){
	Shape *new_shape;
	return new_shape;
}

